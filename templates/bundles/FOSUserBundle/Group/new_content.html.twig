{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_group_new'), 'attr': { 'class': 'fos_user_group_new' } }) }}
    {{ form_widget(form) }}
    <div>
        <input  class="btn btn-success" type="submit" value="{{ 'Créer le groupe'|trans }}" />
    </div>
{{ form_end(form) }}
