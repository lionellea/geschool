{% extends 'template.html.twig' %}
{% block title %}
<title> Classe | Ajout</title>   
{% endblock %}

{% block body %}
   <div class="col-sm-6 eq-box-sm">
                                <div class="panel">
                                    <div class="panel-heading">
                                        <div class="panel-control">
                                            <a class="btn btn-default" href="{{ path('classe_index') }}" title="Retour à la liste"><i class="fa fa-chevron-left"></i></a>
                                           
                                        </div>
                                        <h3 class="panel-title">Ajouter une classe</h3>
                                    </div>
                                    
                                         {{ include('classe/_form.html.twig') }}
                                        

{% endblock %}
