{% extends 'base.html.twig' %}

{% block title %}Acheter{% endblock %}

{% block body %}
    <h1>Acheter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acheter.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ acheter.prix }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ acheter.dateAchat ? acheter.dateAchat|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acheter_index') }}">back to list</a>

    <a href="{{ path('acheter_edit', {'id': acheter.id}) }}">edit</a>

    {{ include('acheter/_delete_form.html.twig') }}
{% endblock %}
